export enum ToolTitle {
    settings = 'Account Settings',
    work = 'Work',
}

export const PathsMap: {
    [key: string]: string
} = {
    '/work/dashboard': '/self-service',
    '/work': '/self-service',
}