/** Small public member projection shared by opportunities member surfaces. */
export interface MemberProfileSummary {
    handle: string
    maxRating?: number
    photoURL?: string
    userId: string
}
