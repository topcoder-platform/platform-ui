import { EnvironmentConfig } from '~/config'

/** Published reviewer qualification guidance on the environment's main site. */
export const REVIEWER_LEARNING_URL = `${EnvironmentConfig.TOPCODER_URL}`
    + '/thrive/articles/Reviewer%20Qualification%20Requirements'

/** Published copilot role guidance on the environment's main site. */
export const COPILOT_LEARNING_URL = `${EnvironmentConfig.TOPCODER_URL}`
    + '/thrive/articles/become-a-copilot-at-topcoder'

/** Published challenge onboarding guidance on the environment's main site. */
export const CHALLENGE_EXPLAINED_URL = `${EnvironmentConfig.TOPCODER_URL}`
    + '/thrive/articles/all-about-topcoder-challenges-tasks-and-gig-work-opportunities'

/** Published design challenge guidance on the environment's main site. */
export const DESIGN_CHALLENGE_LEARNING_URL = `${EnvironmentConfig.TOPCODER_URL}`
    + '/thrive/articles/How%20To%20Compete%20in%20Design'

/** Published checkpoint feedback guidance on the environment's main site. */
export const CHECKPOINT_FEEDBACK_LEARNING_URL = `${EnvironmentConfig.TOPCODER_URL}`
    + '/thrive/articles/how-to-approach-the-checkpoint-feedback-to-decipher-hidden-codes'

/** Published screening guidance on the environment's main site. */
export const SCREENING_LEARNING_URL = `${EnvironmentConfig.TOPCODER_URL}`
    + '/thrive/search?title=How%20to%20Pass%20Screening'
