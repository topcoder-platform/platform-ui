/**
 * Backend resource info
 */
export interface BackendResource {
    id: string
    challengeId: string
    memberId: string
    memberHandle: string
    roleId: string
    createdBy: string
    created: string
    rating: number
}
