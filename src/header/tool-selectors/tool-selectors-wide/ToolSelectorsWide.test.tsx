import '@testing-library/jest-dom'
import { render, RenderResult } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'

import ToolSelectorsWide from './ToolSelectorsWide'

describe('<ToolSelectorsWide />', () => {

    test('it should render the tool selectors in wide format', () => {
        /*         const renderResult: RenderResult = render(
                    <MemoryRouter>
                        <ToolSelectorsWide />
                    </MemoryRouter>
                )
                const headerElement: HTMLElement | null = renderResult.container.querySelector('.tool-selectors-wide')
                expect(headerElement).toBeInTheDocument() */
    })
})
