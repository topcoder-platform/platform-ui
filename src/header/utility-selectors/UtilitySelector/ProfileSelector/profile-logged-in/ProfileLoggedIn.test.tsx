import '@testing-library/jest-dom'
import { render, RenderResult, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'

import ProfileLoggedIn from './ProfileLoggedIn'

describe('<ProfileLoggedIn /> ', () => {

    test('it should display the ProfileLoggedIn', () => {
        /*         const renderResult: RenderResult = render(
                    <MemoryRouter>
                        <ProfileLoggedIn initialized={false} profile={undefined} />
                    </MemoryRouter>
                )
                const ProfileLoggedInElement: HTMLElement | null = renderResult.container.querySelector('.profile-selector')
                expect(ProfileLoggedInElement).toBeNull() */
    })
})
