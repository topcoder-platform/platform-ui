import '@testing-library/jest-dom'
import { render, RenderResult, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'

import ProfileNotLoggedIn from './ProfileNotLoggedIn'

describe('<ProfileNotLoggedIn /> ', () => {

    test('it should display the ProfileNotLoggedIn', () => {
        /*         const renderResult: RenderResult = render(
                    <MemoryRouter>
                        <ProfileNotLoggedIn initialized={false} profile={undefined} />
                    </MemoryRouter>
                )
                const ProfileNotLoggedInElement: HTMLElement | null = renderResult.container.querySelector('.profile-selector')
                expect(ProfileNotLoggedInElement).toBeNull() */
    })
})
