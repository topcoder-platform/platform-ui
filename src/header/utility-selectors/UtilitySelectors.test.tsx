import '@testing-library/jest-dom'
import { render, RenderResult } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'

import UtilitySelectors from './UtilitySelectors'

describe('<UtilitySelectors />', () => {

    test('it should display the utility selectors', () => {
        /*         const renderResult: RenderResult = render(
                    <MemoryRouter>
                        <UtilitySelectors initialized={true} profile={undefined} />
                    </MemoryRouter>
                )
                const utilitiesElement: HTMLElement | null = renderResult.container.querySelector('.utility-selectors')
                expect(utilitiesElement).toBeInTheDocument() */
    })
})
