import { Canvas, Meta, Story, Controls } from '@storybook/blocks';

import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} name="Overview" />

# Button

Button is a clickable interactive element that triggers a response.

You can place text and icons inside of a button.

Buttons are often used for form submissions and to toggle elements into view.

## Usage

The component comes as different styles such as `primary`, `secondary`, `link`;
different variants such as `negative`, `danger` and `warning`;
and have different sizes which you can use to alter the look and feel of the button.

### Button Styles

<Canvas>
    <Story of={ButtonStories.Primary} />
    <Story of={ButtonStories.Secondary} />
    <Story of={ButtonStories.Link} />
</Canvas>

### Button Sizes

<Canvas>
    <Story of={ButtonStories['_hidden_PrimarySmSize']} />
    <Story of={ButtonStories['_hidden_PrimaryMdSize']} />
    <Story of={ButtonStories['_hidden_PrimaryLgSize']} />
    <Story of={ButtonStories['_hidden_PrimaryXlSize']} />
</Canvas>

### Button Variants

#### Light Buttons
<Canvas>
    <Story of={ButtonStories['_hidden_LightPrimary']} />
    <Story of={ButtonStories['_hidden_LightSecondary']} />
    <Story of={ButtonStories['_hidden_LightLink']} />
</Canvas>

#### Danger
<Canvas>
    <Story of={ButtonStories['_hidden_PrimaryDanger']} />
    <Story of={ButtonStories['_hidden_SecondaryDanger']} />
    <Story of={ButtonStories['_hidden_LinkDanger']} />
</Canvas>

#### Full Width Button
Any button style (`primary`, `secondary`, `link`), size (`sm`, `md`, `lg`, `xl`) and variant can take full width (`fullWidth`).

<Canvas>
    <Story of={ButtonStories['_hidden_FullWidth']} />
</Canvas>

### Button with Icon
<Canvas>
    <Story of={ButtonStories.OnlyIcon} />
    <Story of={ButtonStories.WithIconLeft} />
    <Story of={ButtonStories.WithIconRight} />
</Canvas>

## Inputs

Button has the following properties:

<Canvas of={ButtonStories.Primary} />
<Controls />
