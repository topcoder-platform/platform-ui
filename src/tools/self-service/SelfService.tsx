import { FC } from 'react'

import { ContentLayout } from '../../lib'

export const toolTitle: string = 'Self Service'

const SelfService: FC<{}> = () => <ContentLayout title={toolTitle} />

export default SelfService
